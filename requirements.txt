# torch 不在此处固定版本号。PyTorch wheel 与平台/驱动强绑定,
# 需按环境(GPU/CPU、Windows/Linux、CUDA 版本)单独安装,见 README § 安装。
# 直接 `pip install -r requirements.txt` 拉的是 CPU 版,NVIDIA 卡装了也跑不了 GPU。

fastapi==0.115.*
uvicorn[standard]==0.32.*
python-multipart==0.0.*
gensim>=4.3,<5.0  # 替代 fasttext-wheel（官方 fasttext 在 Python 3.13.5 上 pybind11 API 不兼容，编译失败）
python-Levenshtein==0.26.*
numpy>=1.24
pandas>=2.0
openpyxl>=3.1  # xlsx 读写，pandas.to_excel / read_excel 的引擎
pydantic>=2.0
pytest>=8.0
httpx>=0.27
sentence-transformers>=3.0  # BGE-small-zh 默认模型，自动适配 CPU/GPU
